<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%# Show the specific team logo on team pages, otherwise show the NBA logo. %>
    <%= (@team.nil? || @team.id.nil?) ? (link_to image_tag("official-nba.svg", :class=>"nba-header-logo"), root_path) : 
                                        image_tag(@team.logo, :class=>"team-header-logo") %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= (link_to "Home", root_path, :class=>"nav-link") unless (@team.nil? || @team.id.nil?) %>
        </li>

        <li class="nav-item">
          <%= (link_to "Dashboard", admin_dashboard_path, :class=>"nav-link", data: { turbolinks: false }) if current_user.admin %>
        </li>

        <li class="nav-item">
          <%= link_to "Spreadsheet", "https://docs.google.com/spreadsheets/d/1Tr6kYGr_T-DWWXlYs9p5Cc6EXGjUCdJQgSo1plwBklw/edit?usp=sharing", :class=>"nav-link", target: :_blank %>
        </li>
        
        <%= render "partials/user_login" %>
      </ul>

      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>

      <%= render "partials/discord_button" %>
    </div>
  </div>
</nav>
