<turbo-stream action="replace" target="team-assets-<%= @team_number %>">
  <template>
    <%= select_tag "#{@team.name}", options_from_collection_for_select(@team.players, "id", "last_name"), prompt: "Select an Asset", data: { action: "change->trade#loadTeamAssets" } %>
  </template>
</turbo-stream>